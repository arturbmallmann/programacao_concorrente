#include <math.h>

int main(){

int fatia=10;
int N=1000;

int Velho = V[N+2]
int Novo = V[N+2]


V[0] = 0;
V[N+1]
int n = N - 2;
int d = numThreads; //número de threads

fatia = ceil(n/d); 

int inicio = t*fatia + 1;
int fim = inicio + fatia -1;
#if(fim > (N-2)) fim = N - 2;

}

void calc_media(int id, int base, int teto){
	for (int i=base; i < teto; i++){
		Novo[i] = (Velho[i - 1] + Velho[i + 1]) / 2;
	}
}
